#include<stdio.h>
main()
[
 int a[100],max,n,i;
 printf("\n the numbers are...");
 scanf("%d",&n);
 for(i=0;i<=n;i++)
 {
  scanf("%d",a[i]);
  }
  maxa[i]=0;
  for(i=0;i<=n;i++)
  {
   if(a[i]>max)
   {
    max=a[i];
    {
     printf("%d",max[i]);}\
     }
     }
     }
