#include<stdio.h>
int mai()
{
   int i,max=0,a;
   printf("\n Enter 10 numbers");
   for(i=0<i<=10;i++)
   scanf("%d",&a[i]);
   for(i=0;i<=10;i++)
   {
     max=0;
     if(a[i]>max)
     {
      printf("\n maxmimum number is%d", max); 
     }
   }
   return 0;
}
